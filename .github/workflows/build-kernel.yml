name: Build Motorola G45 Fogos Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Kernel Source
        uses: actions/checkout@v4
        with:
          repository: prabhatkrmishra/kernel_motorola_fogos
          ref: 15.0
          fetch-depth: 1
          submodules: recursive

      - name: Install Build Dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            bc bison build-essential flex git curl \
            libelf-dev libssl-dev libncurses-dev \
            python3 clang lld llvm gcc-aarch64-linux-gnu

      - name: Setup Kernel Configuration
        run: |
          mkdir -p out
          # Using the fogos specific defconfig found in your repository
          make O=out ARCH=arm64 vendor/fogos_defconfig

      - name: Compile Kernel
        run: |
          # We use Clang and LLVM as required for modern Motorola kernels
          make -j$(nproc --all) O=out \
            ARCH=arm64 \
            CC=clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            LD=ld.lld \
            LLVM=1 \
            LLVM_IAS=1 2>&1 | tee build.log
          
          # This ensures the workflow reports a failure if compilation stops
          if [ ${PIPESTATUS[0]} -ne 0 ]; then exit 1; fi

      - name: Upload Kernel Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Motorola-G45-Kernel
          path: |
            out/arch/arm64/boot/Image
            out/arch/arm64/boot/dtbo.img
            build.log
